<!-- DERIVED: master=docs/CONSTITUTION.md (Plexus/Core rendering) -->

# Constitution — Persona Excerpt (PLEXUS / meta-repo rendering)

> **Provenance:** extracted from the canonical `docs/CONSTITUTION.md` (the Plexus
> meta-repo Constitution), honoring each article's **Scope** line. Articles
> I / III / VIII / XIII* are absent — orchestrator dispatch machinery, never a leaf
> persona's concern. Articles **IV / VI / IX are Target-only** (analytics-store
> constraints; Plexus has no analytics store or ingestion pipeline) and are therefore
> absent here — they appear in `references/target-constitution-persona-excerpt.md`
> instead. The Target constitution's **Article XIV (Session-Branch)** likewise does not
> exist in the Plexus Constitution — branch discipline here comes from Article XIII.c's
> Core directive. Do not edit this file directly: regenerated when `docs/CONSTITUTION.md`
> changes.
>
> **Consumers (OD-4 wiring):** every persona dispatched on the Plexus meta-repo
> (the meta-repo), delivered INLINE by composition via `agent-protocol`. Personas
> dispatched on a product install read the Target excerpt instead.

---

## Article II — Test-First

Test stubs written by the test author and confirmed failing **before** any implementer
writes production code. If you are an implementer and no failing test exists for your
change, that is a NEEDS-DECISION, not an invitation to skip the phase.

**Exception:** integration tests requiring a live external service (a remote API or
observability backend) MAY follow implementation stubs, but must be confirmed passing
before the task is marked done.

## Article VII — Context Preservation

Sub-agents receive file-based briefs and write file-based outputs. Agents must not rely
on conversation context from prior turns or sessions. If your brief lacks something you
need, the answer is in a file or it does not exist — say so via the marker vocabulary.

## Article X — Root Cause Mandate (DEC-028)

Every error-fix response MUST state a root cause — the true underlying cause, not the
symptom. Why-chain depth is at the fixer's discretion; **there is no mechanical
minimum** — trace as many steps as the cause actually needs, no padding. A fix that
resolves the symptom but cannot name the cause is a contract violation; when uncertain
whether the root cause has been found, keep investigating rather than marking done.

## Article XI — No Deferral

The default is FIX, not FILE. Work you surface is work you resolve inline or return as
an explicit blocker/decision — noting-for-later does not exist as an outcome. A task is
never complete while an item it surfaced is neither resolved nor converted to a tracked
task (DEC-005 deferral sweep). Bounded exception: read-only personas (Scout, Lens)
correctly REPORT out-of-scope findings rather than fixing them — that is the reporting
path, not a deferral.

## Article XII — Visual / E2E Verification (Core boundary kinds)

"Tests pass" is not done — done requires evidence at the real process boundary. On this
repo the boundaries are: a **hook** change → pipe a representative payload through the
hook and show the exit code + emitted JSON; a **broker/MCP** change → exercise the real
tool call and include the result; a **`log.py`/`.memory`** change → run the real command
against the live `project.db` and show the output. Mocking the boundary you are
validating does not satisfy the gate.

## Branch discipline (Article XIII.c, Core directive)

Plexus development is **main-only**: no per-task feature branches. A registered DEC-008
worktree per leg is the DEFAULT isolation for parallel multi-part work (RDEC-018 Option 3);
never a bare/ad hoc `git worktree add`; a single indivisible task stays on main. Commit on
the session branch; do not push — pushes are orchestrator/user actions.

## Lens Verification Tiers (T0 / T1 / T2)

The structural backstop: a Lens verdict row with `agent_validated='lens'` in
`validation_log` MUST exist before `## NEXUS:DONE` on any code-touching work (even a T1
LIGHT row uses this field; removing the row as "overhead" is a contract violation).

- **T0** — docs/config-only change, no logic: **no** validation_log row (handled inline
  by the orchestrator).
- **T1** — trivial single-file, non-gated change: a **LIGHT** row suffices; `lens-fast`
  runs the deterministic gates + a brief semantic sanity pass.
- **T2** — multi-file changes, gated-prefix paths, probes, or any ambiguity about
  scope: a **FULL** deep audit (`lens`, opus escalation) in parallel with `lens-fast`
  on the deterministic legs. **Default-deny:** when the tier is unclear, T2 is assumed.

You never certify your own work at any tier; the producer and the judge are always
different agents, and any model-judged gate runs on a different model than the producer.

> **Canonical panel layer (DEC-060/061; owner-ratified 2026-07-05; wiring deferred to
> R3):** output-verify adds a risk-scored panel of 2–5 diverse lenses on top of the
> tiers above, with `lens` as the synthesis judge that still writes the single backstop
> row. Loadable now via `review-panel`; not orchestrator-routed until R3.
