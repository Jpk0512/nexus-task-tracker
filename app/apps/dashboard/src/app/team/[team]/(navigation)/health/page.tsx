export default function WorkspaceHealthPage() {
	return (
		<div className="mx-auto flex max-w-2xl flex-col gap-3 px-6 py-12">
			<h1 className="font-[510] text-[22px] tracking-[-0.02em]">Workspace Health</h1>
			<p className="text-[13px] text-muted-foreground">Capture debt, unfiled meetings, overdue, stale projects, MCP status — each with a one-click fix.</p>
			<p className="text-[12px] text-muted-foreground/80">
				Shell live — full behavior ships in the next implementation phases.
			</p>
		</div>
	);
}
