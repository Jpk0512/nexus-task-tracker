import { CaptureShell } from "@/components/capture/capture-shell";

/**
 * /capture — Dump | Todos | Outline.
 * Dump is brain-only (not Needs you). Promote is create-then-archive.
 */
export default function CapturePage() {
	return <CaptureShell />;
}
