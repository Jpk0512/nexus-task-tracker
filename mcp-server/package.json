{
  "name": "nexus-mcp",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run server.ts",
    "build": "bun build server.ts --outfile dist/index.js --target bun",
    "check-types": "tsc --noEmit",
    "install:mcp": "bun run install.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.0",
    "nanoid": "^5.1.15",
    "pg": "^8.13.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pg": "^8.20.0"
  }
}
