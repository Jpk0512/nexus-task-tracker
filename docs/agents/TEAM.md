# Agent Team

Project: **Nexus**

Open-source minimalist task/project tracker (Linear-style). Nexus source lives at `app/` under this project root; the rest of the tree is Nexus orchestration scaffolding.

## Personas

| Persona | Role | Owns |
|---------|------|------|
| **forge** | Frontend / TypeScript engineer | `app/` |

## Ownership Rules

- **forge** owns `app/` (the entire Nexus monorepo — apps, packages, supabase). All TypeScript / Next.js / Bun work routes here.
- `nexus-orchestrator`, `scout`, `lens`, `quill`, `quill-py` are core agents shared by every project (see `.claude/agents/`).

---
*Generated by nexus-orchestrator-template/scripts/generate-project.py and edited for Nexus.*
