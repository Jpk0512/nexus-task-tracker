# Same otherwise-valid executor:codex node as good_codex_lane_enabled.yaml. The
# test supplies a codex_lane_flag_path override pointing at a tmp path that does
# NOT exist, so the lane-disabled check must fire.
schema_version: 2
nodes:
  - node_id: N1
    depends_on: []
    downstream_consumers: []
    agent_persona: pipeline-async
    goal: "Confirm executor: codex is rejected when the lane-enabled flag file is absent."
    context_files: []
    acceptance_criteria: ["codex lane flag file absent -> rejected"]
    verification_method:
      type: command
      command: "echo ok"
    risk_tier: T1
    skills_required: [agent-protocol]
    do_not_touch: []
    write_scope: []
    executor: codex
    executor_model: gpt-5.4
    irreversible: false
