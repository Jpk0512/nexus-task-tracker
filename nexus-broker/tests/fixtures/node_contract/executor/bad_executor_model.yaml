# Requires the test to pass a codex_lane_flag_path override pointing at an
# existing tmp file, so only the executor_model check is under test here.
schema_version: 2
nodes:
  - node_id: N1
    depends_on: []
    downstream_consumers: []
    agent_persona: pipeline-async
    goal: "Confirm an unrecognized executor_model slug is rejected for executor: codex."
    context_files: []
    acceptance_criteria: ["executor_model outside the valid slug set is rejected"]
    verification_method:
      type: command
      command: "echo ok"
    risk_tier: T1
    skills_required: [agent-protocol]
    do_not_touch: []
    write_scope: []
    executor: codex
    executor_model: gpt-4
    irreversible: false
