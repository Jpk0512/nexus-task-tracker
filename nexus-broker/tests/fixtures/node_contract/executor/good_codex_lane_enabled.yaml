# Otherwise-fully-valid executor:codex node. The test supplies a
# codex_lane_flag_path override pointing at a tmp file that EXISTS, so the
# lane-enabled check passes and this fixture is expected to validate clean.
schema_version: 2
nodes:
  - node_id: N1
    depends_on: []
    downstream_consumers: []
    agent_persona: pipeline-async
    goal: "Confirm executor: codex passes when the lane-enabled flag file exists."
    context_files: []
    acceptance_criteria: ["codex lane flag file present"]
    verification_method:
      type: command
      command: "echo ok"
    risk_tier: T1
    skills_required: [agent-protocol]
    do_not_touch: []
    write_scope: []
    executor: codex
    executor_model: gpt-5.4
    irreversible: false
